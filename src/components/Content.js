import styled from "styled-components";
import { colors } from "./AuroraTheme/style-data";

const Content = styled.div`
  background: ${colors.neutralGhost.value};
  flex-grow: 1;
  padding: 2rem;
  margin-left: 280px;
`;

export default Content;
